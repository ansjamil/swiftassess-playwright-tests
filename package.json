{
  "name": "swiftassess-signup-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npx playwright install --with-deps",
    "test": "playwright test",
   "test:ci": "playwright test --reporter=junit,html,line --output=test-results",
   "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "dotenv": "^16.6.1"
  }
}
